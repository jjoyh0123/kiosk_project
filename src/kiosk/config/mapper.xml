<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductMapper">

    <select id="getProductById" parameterType="int" resultType="kiosk.config.productVO">
        SELECT productName, productPrice, productCategory, productRecommendStatus, productSaleStatus
        FROM product
        WHERE productIdx = #{productIdx}
    </select>

    <update id="updateProductStatus" parameterType="kiosk.config.productVO">
        UPDATE product
        SET productRecommendStatus = #{productRecommendStatus},
            productSaleStatus = #{productSaleStatus}
        WHERE productIdx = #{idx}
    </update>

</mapper>
